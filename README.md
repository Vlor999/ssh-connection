# ssh-connection
Where you'll have all informations about how to run your ssh-connection

## Commands
You must have your the two computers switched on. 
Then to connect you must have the `tailscale` application launched and on.
Then you must just look at `IP` provided in here. 

To connect : 
```bash
ssh willem@0.0.0.0
```

